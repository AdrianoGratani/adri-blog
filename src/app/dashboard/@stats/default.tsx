import ParallelCard from "@/components/ParallelCard";

export default function DefaultStats() {

  return (
    <>
      <ParallelCard>
        Stats: parallel page (default: you are in nested url path, this is fallback UI to prevent errors on refresh)
      </ParallelCard>
    </>
  )
}
