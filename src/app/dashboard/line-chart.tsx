export default function LineChart() {

    return (
        <div>
            linechart component, NOT page.tsx
            THIS IS IMPORTED FROM SAME FOLDER BUT ANOTHER FILE
            you can access it only if the folder has a page.tsx,
            and the page.tsx imports it.
        </div>
    )
}